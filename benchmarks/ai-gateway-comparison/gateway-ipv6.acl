mode { kind = "standalone" }

# IPv6 loopback transport dimension. Upstream stays on [::1] as well so the
# measured path is not a mixed v4/v6 hop.
entrypoints "ai" {
  address = "[::1]:18101"
}

routers "ai-benchmark" {
  rule        = "PathPrefix(`/`)"
  service     = "ai-benchmark"
  entrypoints = ["ai"]
}

services "ai-benchmark" {
  load_balancer {
    strategy             = "round-robin"
    request_timeout      = "120s"
    stream_idle_timeout  = "120s"
    stream_total_timeout = "120s"
    servers = [{ url = "http://[::1]:18100" }]
  }
}

observability {
  metrics_enabled    = false
  access_log_enabled = false
  tracing_enabled    = false
}
